import { FileType } from "./types";
declare type Props = {
    data: FileType;
    filesChanged?: string[];
    maxDepth: number;
    colorEncoding: "type" | "number-of-changes" | "last-change";
    customFileColors?: {
        [key: string]: string;
    };
};
export declare function createTree(): Promise<({ data, filesChanged, maxDepth, colorEncoding, customFileColors, }: Props) => JSX.Element>;
export {};
