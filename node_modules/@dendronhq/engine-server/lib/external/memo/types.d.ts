import { URI } from "vscode-uri";
export declare type WorkspaceCache = {
    imageUris: URI[];
    markdownUris: URI[];
    otherUris: URI[];
    allUris: URI[];
    danglingRefs: string[];
    danglingRefsByFsPath: {
        [key: string]: string[];
    };
};
export declare type RefT = {
    label: string;
    ref: string;
};
