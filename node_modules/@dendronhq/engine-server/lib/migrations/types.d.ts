import { IntermediateDendronConfig, DendronError, WorkspaceSettings } from "@dendronhq/common-all";
import { WorkspaceService } from "../workspace";
export * from "./service";
export declare type MigrateFunction = (opts: {
    dendronConfig: IntermediateDendronConfig;
    wsConfig?: WorkspaceSettings;
    wsService: WorkspaceService;
}) => Promise<{
    error?: DendronError;
    data: {
        dendronConfig: IntermediateDendronConfig;
        wsConfig?: WorkspaceSettings;
    };
}>;
export declare type MigrationChangeSet = {
    name: string;
    func: MigrateFunction;
};
export declare type Migrations = {
    version: string;
    changes: MigrationChangeSet[];
};
export declare type MigrationChangeSetStatus = {
    error?: DendronError;
    data: {
        version: string;
        changeName: string;
        status: "ok" | "error";
        dendronConfig: IntermediateDendronConfig;
        wsConfig?: WorkspaceSettings;
    };
};
