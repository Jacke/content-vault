import { DEngine, Position } from "@dendronhq/common-all";
import { HashTag } from "../types";
import { Decorator } from "./utils";
import { DecorationWikilink } from "./wikilinks";
export declare type DecorationHashTag = DecorationWikilink & {
    color?: string;
};
export declare function isDecorationHashTag(decoration: DecorationWikilink): decoration is DecorationHashTag;
export declare const decorateHashTag: Decorator<HashTag, DecorationHashTag>;
export declare function decorateTag({ fname, engine, position, lineOffset, }: {
    fname: string;
    engine: DEngine;
    position: Position;
    lineOffset?: number;
}): Promise<{
    errors: import("@dendronhq/common-all").IDendronError<import("@dendronhq/common-all").StatusCodes | undefined>[];
    decorations: DecorationHashTag[];
}>;
