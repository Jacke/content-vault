import { DEngine, VSRange } from "@dendronhq/common-all";
import { Decoration, DECORATION_TYPES } from "./utils";
export declare type DecorationTaskNote = Decoration & {
    type: DECORATION_TYPES.taskNote;
    beforeText?: string;
    afterText?: string;
};
/** Decorates the note `fname` in vault `vaultName` if the note is a task note. */
export declare function decorateTaskNote({ engine, range, fname, vaultName, }: {
    engine: DEngine;
    range: VSRange;
    fname: string;
    vaultName?: string;
}): Promise<DecorationTaskNote | undefined>;
